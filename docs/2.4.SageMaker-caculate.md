# Chi Tiết: Làm Sạch Dữ Liệu (Data Cleaning) cho Phân Tích RFM

## Tổng Quan

Trước khi tính toán các chỉ số RFM, chúng ta cần **làm sạch dữ liệu** (data cleaning) để:
- ✅ Loại bỏ các ghi chép không cần thiết
- ✅ Xóa cột không sử dụng
- ✅ Loại bỏ bản sao
- ✅ Chuyển đổi định dạng dữ liệu
- ✅ Xử lý giá trị NULL

Đây là bước **rất quan trọng** vì dữ liệu chưa được làm sạch sẽ dẫn đến kết quả phân tích sai lệch.

---

## Tóm Tắt Quy Trình

```
DỮ LIỆU GỐC
    ↓
① LỌC EVENT_TYPE = 'PURCHASE'
    ↓
② XÓA CỘT KHÔNG CẦN THIẾT
    ↓
③ LOẠI BỎ BẢN SÁO
    ↓
④ CHUYỂN ĐỔI DATETIME
    ↓
⑤ KIỂM TRA NULL
    ↓
DỮ LIỆU SẠCH sẵn sàng cho phân tích RFM
```

---

## Code Đầy Đủ 

```python
# select customers who purchased
df_sales=df.loc[df.event_type=='purchase',:]

# drop "category_code", "brand", "product_id", "category_id", and "user_session"
df_sales=df_sales.drop(columns=['category_code','brand','product_id','category_id','user_session'])

# drop duplicates
df_sales=df_sales.drop_duplicates()

# convert "event_time" to DateTime format
df_sales['event_time']=pd.to_datetime(df_sales['event_time'])

nullcolumns=df_sales.isnull().sum()
nullnumbers=len(nullcolumns[nullcolumns!=0])
print("After data selection and cleansing, the dataframe has {} rows, {} columns, and {} null value.\n".format(df_sales.shape[0],df_sales.shape[1],nullnumbers))
print("Shown below are the first 3 rows of the cleaned dataframe:\n")
display(df_sales.head(3))
```

## Phần 1: Lọc Chỉ Ghi Chép "Purchase" (Mua Hàng)

### Code:
```python
# Chỉ chọn các ghi chép có event_type = 'purchase'
df_sales = df.loc[df.event_type == 'purchase', :]
```

### Giải Thích Chi Tiết:

#### **Điều Gì Xảy Ra?**

```
DataFrame Gốc (df):
┌─────────────────────────────────────┐
│ event_type | user_id | event_time  │
├─────────────────────────────────────┤
│ view       | 123     | 2024-01-01  │  ❌ Loại bỏ (chỉ xem)
│ cart       | 123     | 2024-01-02  │  ❌ Loại bỏ (thêm vào giỏ)
│ purchase   | 123     | 2024-01-03  │  ✅ Giữ lại (mua hàng)
│ view       | 456     | 2024-01-04  │  ❌ Loại bỏ
│ purchase   | 456     | 2024-01-05  │  ✅ Giữ lại
└─────────────────────────────────────┘

            ↓

DataFrame Sau Lọc (df_sales):
┌─────────────────────────────────────┐
│ event_type | user_id | event_time  │
├─────────────────────────────────────┤
│ purchase   | 123     | 2024-01-03  │
│ purchase   | 456     | 2024-01-05  │
└─────────────────────────────────────┘
```

---

## Phần 2: Xóa Cột Không Cần Thiết

### Code:
```python
# Xóa các cột không sử dụng
df_sales = df_sales.drop(
    columns=['category_code', 'brand', 'product_id', 'category_id', 'user_session']
)
```

### Giải Thích Chi Tiết:

#### **Tại Sao Phải Xóa Cột?**

| Cột | Lý Do Xóa |
|-----|----------|
| **category_code** | Không cần cho tính RFM (chỉ cần user_id, event_time, price) |
| **brand** | Không cần cho tính RFM |
| **product_id** | Không cần cho tính RFM |
| **category_id** | Không cần cho tính RFM |
| **user_session** | Không cần cho tính RFM |

```
Trước xóa:
┌─────────────────────────────────────────────────────────────────────┐
│ user_id | event_type | event_time | price | category_code | brand  │
├─────────────────────────────────────────────────────────────────────┤
│ 123     | purchase   | 2024-01-03 | 100   | electronics   | Sony   │
│ 456     | purchase   | 2024-01-05 | 250   | fashion       | Nike   │
└─────────────────────────────────────────────────────────────────────┘
            ↓
Sau xóa:
┌──────────────────────────────────────────────┐
│ user_id | event_type | event_time | price   │
├──────────────────────────────────────────────┤
│ 123     | purchase   | 2024-01-03 | 100     │
│ 456     | purchase   | 2024-01-05 | 250     │
└──────────────────────────────────────────────┘
```

---

## Phần 3: Loại Bỏ Bản Sao (Duplicates)

### Code:
```python
# Loại bỏ các hàng trùng lặp
df_sales = df_sales.drop_duplicates()
```

### Giải Thích Chi Tiết:

```
DataFrame có bản sao:
┌────────────────────────────────────────────┐
│ user_id | event_time | price              │
├────────────────────────────────────────────┤
│ 123     | 2024-01-03 | 100   ✅ Hàng 1    │
│ 123     | 2024-01-03 | 100   ❌ BẢN SAO   │  ← Hàng này giống hàng 1
│ 456     | 2024-01-05 | 250   ✅ Hàng 2    │
│ 456     | 2024-01-05 | 250   ❌ BẢN SAO   │  ← Hàng này giống hàng 2
└────────────────────────────────────────────┘
            ↓ Sau drop_duplicates()
┌────────────────────────────────────────────┐
│ user_id | event_time | price              │
├────────────────────────────────────────────┤
│ 123     | 2024-01-03 | 100   ✅ Giữ lại  │
│ 456     | 2024-01-05 | 250   ✅ Giữ lại  │
└────────────────────────────────────────────┘
```

---

## Phần 4: Chuyển Đổi Định Dạng Thời Gian (DateTime)

### Code:
```python
# Chuyển đổi cột "event_time" thành định dạng DateTime
df_sales['event_time'] = pd.to_datetime(df_sales['event_time'], infer_datetime_format=True)
```

### Giải Thích Chi Tiết:

#### **Tại Sao Phải Chuyển Đổi?**

| Lý Do | Giải Thích |
|------|-----------|
| **Tính toán Recency** | Cần trừ ngày (datetime - datetime) |
| **Sắp xếp theo thời gian** | Không thể sắp xếp string như datetime |
| **Lọc theo ngày** | Dễ dàng lọc dữ liệu từ ngày X đến Y |
| **Định dạng chuẩn** | Loại bỏ các định dạng không chuẩn |

#### **So Sánh Trước và Sau:**

```python
# Trước chuyển đổi
print(df_sales['event_time'].dtype)  # ← object (string)
print(df_sales['event_time'].head())
# Output: 
# 0    2024-01-03 10:30:45
# 1    2024-01-05 14:20:15
# 2    2024-01-07 08:15:00

# ❌ Không thể tính toán: datetime - datetime không hoạt động
try:
    days_ago = datetime.now() - df_sales['event_time'].iloc[0]
    print(days_ago)
except Exception as e:
    print(f"Lỗi: {e}")


# Sau chuyển đổi
df_sales['event_time'] = pd.to_datetime(df_sales['event_time'], infer_datetime_format=True)

print(df_sales['event_time'].dtype)  # ← datetime64[ns]
print(df_sales['event_time'].head())
# Output:
# 0   2024-01-03 10:30:45
# 1   2024-01-05 14:20:15
# 2   2024-01-07 08:15:00

# ✅ Có thể tính toán
days_ago = datetime.now() - df_sales['event_time'].iloc[0]
print(f"Days ago: {days_ago.days}")  # ← Hoạt động!
```

---

## Phần 5: Kiểm Tra Giá Trị NULL

### Code:
```python
# Kiểm tra giá trị NULL
nullcolumns = df_sales.isnull().sum()
nullnumbers = len(nullcolumns[nullcolumns != 0])

print("Sau khi làm sạch dữ liệu, DataFrame có {} rows, {} columns, và {} null values.\n".format(
    df_sales.shape[0], df_sales.shape[1], nullnumbers))
```

### Giải Thích Chi Tiết:

#### **Phân Tích Chi Tiết Code:**

```python
# Step 1: Tìm số NULL trong mỗi cột
nullcolumns = df_sales.isnull().sum()
# Result: Series chứa số NULL cho mỗi cột
# user_id        0
# event_time     0
# price          2
# dtype: int64

print(nullcolumns)

# Step 2: Lọc chỉ cột có NULL
nullcolumns[nullcolumns != 0]
# Result:
# price    2
# dtype: int64

# Step 3: Đếm số cột có NULL
nullnumbers = len(nullcolumns[nullcolumns != 0])
# Result: 1 (chỉ cột 'price' có NULL)

print(f"Số cột có NULL: {nullnumbers}")
```
---

## Phần 6: Hiển Thị Kết Quả Cuối Cùng

### Code:
```python
print("After data selection and cleansing, the dataframe has {} rows, {} columns, and {} null value.\n".format(
    df_sales.shape[0], df_sales.shape[1], nullnumbers))
print("Shown below are the first 3 rows of the cleaned dataframe:\n")
display(df_sales.head(3))
```

---

**Bước tiếp theo:** Hãy chuyển sang phần [Data Eploration](2.5.SageMaker-EDA.md)! 🚀