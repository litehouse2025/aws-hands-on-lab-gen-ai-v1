# AWS Bedrock Agents: Xây Dựng AI Agent cho Marketing

## Giới Thiệu

Phần tiếp theo của bài lab sẽ sử dụng dữ liệu đã phân tích từ SageMaker ở phần trước, kết hợp với dữ liệu sản phẩm để **xây dựng AI Agent giúp đưa ra đề xuất tiếp thị (marketing recommendations)** cho các phân khúc khách hàng khác nhau.

Trước tiên, chúng ta hãy làm quen với tính năng **Agents trong AWS Bedrock**.

---

## Tính Năng Agents trên AWS Bedrock

### **AWS Bedrock Là Gì?**

**Định nghĩa:**
```
AWS Bedrock = Dịch vụ cung cấp Foundation Models (LLMs) qua API
• Không cần quản lý máy chủ
• Sử dụng trực tiếp từ AWS Console hoặc SDK
• Hỗ trợ nhiều mô hình: Claude, Llama, Mistral, ...
```

**Ưu điểm:**
```
✅ Dễ sử dụng - Chỉ cần API call
✅ An toàn - Data không được lưu trữ riêng
✅ Linh hoạt - Chọn model phù hợp
✅ Giá rẻ - Trả tiền theo usage
✅ Tích hợp AWS - Dễ kết nối S3, Lambda, ...
```

### **Agents Là Gì?**

**Định nghĩa:**

```
Agents = AI Assistants có khả năng:
  1. Suy luận độc lập (Reasoning)
  2. Lập kế hoạch hành động (Planning)
  3. Sử dụng các công cụ (Tools)
  4. Tương tác với hệ thống bên ngoài
  5. Đưa ra quyết định và hành động
```

**So sánh LLM vs Agent:**

| Tính Năng | LLM Thông Thường | Agent |
|-----------|------------------|-------|
| **Suy luận** | Trả lời theo prompt | Suy luận độc lập |
| **Hành động** | Chỉ tạo text | Thực hiện actions |
| **Tools** | Không | Có (DB, API, S3, ...) |
| **Planning** | Không | Lập kế hoạch multi-step |
| **Lặp lại** | Một lần | Cho đến khi hoàn thành |
| **Độc lập** | Phụ thuộc prompt | Tự chủ động |

**Ví dụ:**

```
LLM:
  Prompt: "Tìm sản phẩm phù hợp cho khách hàng này"
  Output: "Sản phẩm A, B, C có thể phù hợp"
  (Chỉ là đề xuất text)

Agent:
  Yêu cầu: "Tìm sản phẩm phù hợp cho khách hàng này"
  Hành động:
    1. Truy vấn customer data từ database
    2. Lấy lịch sử mua hàng
    3. So sánh với product catalog
    4. Tính toán match score
    5. Gửi email khuyến nghị
    6. Cập nhật tracking
  (Thực hiện toàn bộ quy trình)
```

---

## Ứng Dụng Agents cho Marketing

### **Trường Hợp Sử Dụng (Use Cases):**

**1️⃣ Gợi Ý Sản Phẩm (Product Recommendations)**

```
Input: Customer segment (RFM)
Process:
  • Phân tích RFM segment (Loyal, At-Risk, ...)
  • Xem product category phù hợp
  • So sánh với purchase history
  • Rank theo relevance score
Output: Top 3-5 sản phẩm được đề xuất
```

**2️⃣ Chiến Lược Tiếp Thị (Marketing Strategy)**

```
Input: Customer segment + Product catalog
Process:
  • Xác định pain points của segment
  • Tìm giải pháp trong product catalog
  • Tạo messaging phù hợp
  • Lập kế hoạch campaign
Output: Marketing strategy & messaging
```

**3️⃣ Phân Tích Khách Hàng (Customer Analysis)**

```
Input: Customer data + Historical data
Process:
  • Phân tích hành vi (Behavior)
  • Xác định churn risk
  • Tìm upsell/cross-sell opportunities
  • Dự đoán lifetime value
Output: Customer insights & recommendations
```

**4️⃣ Tự Động Hóa Quy Trình (Process Automation)**

```
Input: Customer requests
Process:
  • Xử lý yêu cầu tự động
  • Lấy dữ liệu từ nhiều nguồn
  • Ra quyết định dựa trên rules
  • Thực hiện hành động
Output: Completed actions + Confirmations
```

---

**🎯 Tiếp theo** Bắt đầu với Bedrock [phần tiếp theo](3.1.Bedrock-start.md) 🚀 
